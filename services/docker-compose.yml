gdax:
    build: ./gdax-ws
    restart: always
    container_name: GDAX_WS

coincap:
    build: ./coincap-ws
    restart: always
    container_name: COINCAP_WS

iex:
    build: ./iex-ws
    restart: always
    container_name: IEX_WS

mktclose:
    build: ./mkt-close
    restart: always
    container_name: MKT_CLOSE

cryptoclose:
    build: ./crypto-close
    restart: always
    container_name: CRYPTO_CLOSE
